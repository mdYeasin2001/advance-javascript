================= undefined ================
let age;

function add(num1, num2){
    console.log(num1 + num2);
    // return
}

function add(num1, num2){
    return num1, num2;
}

const myObj = {nam: "jantu", phone: 01344}
const result = myObj.gf;
console.log(result);

const myArray = [3, 5, 9];
console.log(myArray[5]);

const setValue = undefined; // undefined value should not be set by developer;
console.log(setValue);

===================== null ==================
let name = null; // if needed developer can set a null value;
console.log(name);

// undefined: when we try to get or access a value that not set yet than we get undefined error. there are some example above 

// null: null means empty, means there is no value;